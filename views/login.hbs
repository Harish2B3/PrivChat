<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PrivChat - Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Global Styles */
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #ff6e7f);
            color: #fff;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            background: linear-gradient(135deg, #6e7aeb, #f9715f);
            border-radius: 12px;
            padding: 40px;
            width: 100%;
            max-width: 450px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            transition: background 0.5s ease;
        }

        .card h2 {
            font-size: 2.5em;
            font-weight: 600;
            color: #fff;
            margin-bottom: 30px;
            text-align: center;
        }

        .form-label {
            font-weight: bold;
            font-size: 1.2em;
            color: #f8f8f8;
        }

        .form-control {
            background-color: #fff;
            border: 1px solid #444;
            color: #333;
            border-radius: 8px;
            padding: 12px;
            font-size: 1.1em;
        }

        .form-control:focus {
            border-color: #ff9a9e;
            background-color: #f4f4f4;
            box-shadow: 0 0 5px rgba(255, 154, 158, 0.5);
        }

        .btn-primary {
            background-color: #ff9a9e;
            border-color: #ff9a9e;
            color: #fff;
            padding: 12px;
            border-radius: 8px;
            font-size: 1.2em;
            width: 100%;
            cursor: pointer;
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #f9715f;
            border-color: #f9715f;
        }

        a {
            color: #f8f8f8;
            text-decoration: none;
            font-size: 1.1em;
            font-weight: 600;
            text-align: center;
            display: block;
            margin-top: 20px;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #ff9a9e;
        }

        .alert-danger {
            background-color: #f87171;
            color: #fff;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
        }

    </style>
</head>

<body>

    <div class="container d-flex justify-content-center align-items-center vh-100">

        <div class="card p-4 shadow">
            {{#if message}}
                <div class="alert alert-danger" role="alert">
                    {{message}}
                </div>
            {{/if}}

<!-- Your login form here -->


            <h2>Login</h2>

            <form method="post">
                <div class="mb-3">
                    <label for="username" class="form-label">Username</label>
                    <input type="text" name="un" class="form-control" id="username" placeholder="Enter your username" required>
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" name="pass" class="form-control" id="password" placeholder="Enter your password" required>
                </div>

                <button type="submit" class="btn btn-primary w-100">Login</button>
                <a href="/register" class="d-block text-center mt-3">Don't have an account?</a>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS Bundle (including Popper.js) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>
